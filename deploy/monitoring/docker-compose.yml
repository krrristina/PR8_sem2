services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8443:8443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./cert.pem:/etc/nginx/tls/cert.pem
      - ./key.pem:/etc/nginx/tls/key.pem
    extra_hosts:
      - "host.docker.internal:host-gateway"